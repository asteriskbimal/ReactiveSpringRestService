language: java
sudo: required
services:
  - docker
script:
  -  docker build -t bimal/java -f Dockerfile.dev .
after_success:
  # Take those images and push them to docker hub
  - docker tag imageHash gcr.io/bimal-157419/bimal/java:latest
  - docker push gcr.io/bimal-157419/bimal/java:latest
